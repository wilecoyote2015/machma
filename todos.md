- PDF- und Excel-Export der ganzen Tabellen und der Helferliste
- add optional color field to helper data. Used to draw color of assignee badge in nodes
- Refactor and cleanup! There are many duplicates! Need more constants instead of repeating stuff - in particular possible values and file structures! Good example: in TaskTableView, the possible taks statuses are hardcoded!!! I bet they are at other places, too! Also elements like dropdowns are styled indivdually instead of using a shared dropdown style! Those are only examples! Look for duplications and bad style in the codebase! Clean, high-quality and maintainable code is the goal!
- BUG: Title of Task in detail panel is not editable
- BUG: filtering by groups in timeline view behaves glitchy sometimes In tasks table, it works fine. In timeline, for some groups or when switching back and forth between groups, sometimes no nodes or only singe nodes are shown.
The timeline arrow sometimes even disappears!

- The whole automatic zooming and viewport logic / fitting in timeline view is buggy and somewhat annoying: when selecting a group, it force-fits viewport. when removing the groups filter by deselecting all groups, no fitting occurs.
I think it is best not to do the auto fitting - as there is already a button for manual fitting and we can make a shortcut in the future (not now). Core issue is that the timeline axis only spans the visible nodes. So if suddenly only nodes out of viewport exist, then the timeline is not in viewport and user loses orientation. So instead of auto-fitting (revert this), ensure the timeline axis is always drawn from first to last date within the whole task set, not only the visible nodes. And the viewport does not aut-adjust so that the user keeps orientation in time. But still adjust the ticks dynamically as you do now. E.g. if there is only a cluster of nodes in the middle of the viewport, then there are the date ticks there. But the whole timeline with start and end date ticks is always drawn. You have also introduced a regression where the timemline currently stops at some date before last date.that's a bug.